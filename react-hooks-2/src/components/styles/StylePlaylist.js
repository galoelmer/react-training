import { makeStyles } from '@material-ui/core/styles';

const StylePlaylist = makeStyles({
  root: {
    display: 'flex',
    flexDirection: 'column',
    overflow: 'hidden',
    color: '#000',
  },
});

export default StylePlaylist;
