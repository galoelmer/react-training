import { makeStyles } from '@material-ui/core/styles';

const stylePlaylistItems = makeStyles({
  root: {
    padding: '0 20px',
    'overflow-y': 'auto',
  },
});

export default stylePlaylistItems;
